import React from 'react';
import { useTranslation } from 'react-i18next';

const Game: React.FC = () => {
  const { t } = useTranslation();

  return (
    <div>
      Game
    </div>
  );
};

export default Game;
